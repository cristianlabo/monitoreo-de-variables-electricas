# Ruta relativa: Vercel hace proxy a Oracle via rewrites en next.config.js
NEXT_PUBLIC_API_HOST_URL=/prueba

# URL del backend en Oracle (solo servidor, no se expone al navegador)
BACKEND_URL=http://163.176.32.93:3000
