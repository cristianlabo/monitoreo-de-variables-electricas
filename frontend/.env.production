# Producción (Vercel): ruta relativa, el proxy está en next.config.js
NEXT_PUBLIC_API_HOST_URL=/prueba

# Backend Oracle (solo servidor Vercel, no se expone al navegador)
BACKEND_URL=https://163.176.32.93:3000
